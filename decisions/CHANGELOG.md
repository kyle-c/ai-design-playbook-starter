# Changelog

> Auto-regenerated weekly from `/decisions/log.json`. Most recent first.

## Week of 2026-04-27

### `library` Adopt AI Design Playbook structure
Codebase becomes the single source of truth for the design system; skill files persist context across Claude sessions.

### `token` Placeholder color primitives — replace before first flow
Generic Indigo + Zinc baseline ships with the scaffold. Replace with real brand identity (and re-run the token-level a11y audit) before building any user-facing screens.
